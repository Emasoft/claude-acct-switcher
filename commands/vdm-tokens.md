---
description: Show recent token usage across all saved Van Damme-o-Matic accounts
allowed-tools: Bash(vdm tokens)
---

Print recent token usage broken down by account, model, repo, and
branch. Pulls from the dashboard's `/api/token-usage` snapshot.
Requires the dashboard to be running.

!`vdm tokens`
