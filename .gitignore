# Account credentials (NEVER commit these)
accounts/
*.label

# Runtime config and data
config.json
activity-log.json
account-state.json
utilization-history.json
probe-log.json
token-usage.json
session-history.json
account-prefs.json
viewer-state.json
.dashboard.pid
.dashboard.lock
events.jsonl
events.jsonl.*

# Logs
*.log

# OS files
.DS_Store
Thumbs.db

# Node
node_modules/
/reports/
/reports_dev/

# Dev-only scratch (per global rule 0.2 — counterparts of published dirs)
/docs_dev/
/scripts_dev/
/samples_dev/
/examples_dev/
/tests_dev/
/downloads_dev/
/libs_dev/
/builds_dev/

# Internal review artifacts (contain absolute paths, internal analysis)
/reviews/

# Claude Code per-session / per-user state
/.claude/chat_history/
/.claude/scheduled_tasks.json
/.claude/scheduled_tasks.lock
/.claude/settings.local.json

# Janitor / rechecker / tldr plugin runtime state
/.janitor/
/.rechecker/
/.tldr/

# Stray output files from `tokf` typos / mishaps (e.g. `tokf raw 1` → `1tokf`)
/1tokf
/[0-9]*tokf
/.claude/worktrees/
